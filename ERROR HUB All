local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/x2zu/OPEN-SOURCE-UI-ROBLOX/refs/heads/main/X2ZU%20UI%20ROBLOX%20OPEN%20SOURCE/DummyUi-leak-by-x2zu/fetching-main/Tools/Framework.luau"))()

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

local Window = Library:Window({
    Title = "ERROR HUB",
    Desc = "Beta!!!!",
    Icon = 105059922903197,
    Theme = "Dark",
    Config = {
        Keybind = Enum.KeyCode.LeftControl,
        Size = UDim2.new(0, 450, 0, 350)
    },
    CloseUIButton = {
        Enabled = true,
        Text = "ERROR"
    }
})

local humanoid
local function updateHumanoid()
    local char = player.Character
    if char then
        humanoid = char:FindFirstChildOfClass("Humanoid")
    end
end
updateHumanoid()

local speedEnabled, jumpEnabled, noclipEnabled, followEnabled = false, false, false, false
local speedValue, jumpValue, followDistance = 16, 50, 16
local followMode, followTarget, followTween = "Tween", nil, nil

local function reapplySettings()
    task.wait(1.5)
    if not player.Character then return end
    humanoid = player.Character:FindFirstChildOfClass("Humanoid")

    if speedEnabled and humanoid then
        humanoid.WalkSpeed = speedValue
    end

    if jumpEnabled and humanoid then
        humanoid.UseJumpPower = true
        humanoid.JumpPower = jumpValue
    end

    if noclipEnabled then
        task.spawn(function()
            while noclipEnabled and task.wait(0.05) do
                if not player.Character then break end
                for _, part in pairs(player.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end

    if followEnabled and followTarget then
        task.spawn(function()
            while followEnabled and task.wait(0.01) do
                if not (player.Character and followTarget.Character) then break end
                local myRoot = player.Character:FindFirstChild("HumanoidRootPart")
                local targetRoot = followTarget.Character:FindFirstChild("HumanoidRootPart")
                if not (myRoot and targetRoot) then break end

                local targetY = targetRoot.Position.Y
                local behindVector = -targetRoot.CFrame.LookVector * followDistance
                local targetPos = targetRoot.Position + behindVector
                local newPos = Vector3.new(targetPos.X, targetY, targetPos.Z)
                local lookAt = Vector3.new(targetRoot.Position.X, targetY, targetRoot.Position.Z)
                local newCFrame = CFrame.new(newPos, lookAt)

                if followMode == "Teleport" then
                    myRoot.CFrame = newCFrame
                elseif followMode == "Tween" then
                    if followTween and followTween.PlaybackState == Enum.PlaybackState.Playing then continue end
                    local tweenInfo = TweenInfo.new(0.02, Enum.EasingStyle.Linear)
                    followTween = TweenService:Create(myRoot, tweenInfo, {CFrame = newCFrame})
                    followTween:Play()
                end
            end
        end)
    end
end

player.CharacterAdded:Connect(function(char)
    humanoid = char:WaitForChild("Humanoid")

    humanoid.Died:Connect(function()
        Window:Notify({
            Title = "💀 ตัวละครตาย",
            Desc = "รีเซ็ตระบบทั้งหมด...",
            Time = 3
        })
    end)

    reapplySettings()
end)

local MainTab = Window:Tab({Title = "Main", Icon = "zap"}) do
    MainTab:Section({Title = ""})

    MainTab:Toggle({
        Title = "🏃 วิ่งเร็ว",
        Value = false,
        Callback = function(v)
            speedEnabled = v
            if humanoid then humanoid.WalkSpeed = v and speedValue or 16 end
        end
    })

    MainTab:Slider({
        Title = "⚡ ปรับความเร็ว",
        Min = 16, Max = 200, Value = 50,
        Callback = function(val)
            speedValue = val
            if speedEnabled and humanoid then humanoid.WalkSpeed = val end
        end
    })

    MainTab:Toggle({
        Title = "🦘 กระโดดสูง",
        Value = false,
        Callback = function(v)
            jumpEnabled = v
            if humanoid then
                humanoid.UseJumpPower = true
                humanoid.JumpPower = v and jumpValue or 50
            end
        end
    })

    MainTab:Slider({
        Title = " ปรับความสูง",
        Min = 50, Max = 300, Value = 100,
        Callback = function(val)
            jumpValue = val
            if jumpEnabled and humanoid then humanoid.JumpPower = val end
        end
    })

    MainTab:Toggle({
        Title = "ทะลุสิ่งของ",
        Value = false,
        Callback = function(v)
            noclipEnabled = v
            if v then
                Window:Notify({Title = "✅ เปิดทะลุสิ่งของ", Desc = "ตอนนี้มึงทะลุได้ละ!", Time = 3})
            else
                Window:Notify({Title = "🛑 ปิดฟังชั่นทะลุ", Desc = "กลับสู่สภาพปกติละ", Time = 3})
            end
        end
    })

    RunService.Stepped:Connect(function()
        if not noclipEnabled then return end
        local char = player.Character
        if not char then return end
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end)
end

local TPTab = Window:Tab({Title = "TP", Icon = "map-pin"})

local selectedPlayerName, playerNames = "", {}
local function UpdatePlayerList()
    playerNames = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(playerNames, p.DisplayName .. " (" .. p.Name .. ")")
        end
    end
end
UpdatePlayerList()
Players.PlayerAdded:Connect(UpdatePlayerList)
Players.PlayerRemoving:Connect(UpdatePlayerList)

TPTab:Textbox({
    Title = "พิมชื่อผู้เล่นที่จะวาร์ปหา",
    Placeholder = "(แค่ 3 ตัวแรกก็พอ)",
    Callback = function(text)
        selectedPlayerName = text
    end
})

TPTab:Dropdown({
    Title = "🎯 รูปแบบการตาม",
    List = {"Tween", "Teleport"},
    Value = followMode,
    Callback = function(val)
        followMode = val
        Window:Notify({
            Title = "🔧 Mode Set",
            Desc = "โหมดติดตาม: " .. val,
            Time = 2
        })
    end
})

TPTab:Slider({
    Title = "ระยะ ",
    Min = 1, Max = 50, Value = followDistance,
    Callback = function(val)
        followDistance = val
    end
})

TPTab:Toggle({
    Title = "🔁 ตามคนที่มึงเลือก",
    Value = false,
    Callback = function(v)
        followEnabled = v
        if v then
            local input = string.lower(selectedPlayerName)
            if #input < 3 then
                Window:Notify({Title = "❌ Error", Desc = "กรุณาพิมพ์อย่างน้อย 3 ตัวอักษร", Time = 3})
                followEnabled = false
                return
            end

            followTarget = nil
            for _, p in pairs(Players:GetPlayers()) do
                local name, displayName = string.lower(p.Name), string.lower(p.DisplayName)
                if name:sub(1, #input) == input or displayName:sub(1, #input) == input then
                    followTarget = p
                    break
                end
            end

            if not followTarget or not followTarget.Character or not followTarget.Character:FindFirstChild("HumanoidRootPart") then
                Window:Notify({Title = "❌ Error", Desc = "ไม่พบผู้เล่นที่จะตาม", Time = 3})
                followEnabled = false
                return
            end

            Window:Notify({Title = "✅ เริ่มตาม", Desc = "กำลังตาม " .. followTarget.DisplayName .. " ด้วยโหมด " .. followMode, Time = 4})
            reapplySettings()
        else
            followTarget = nil
            followTween = nil
            Window:Notify({Title = "🛑 หยุดตาม", Desc = "หยุดตามผู้เล่นแล้ว", Time = 3})
        end
    end
})

TPTab:Button({
    Title = "🚀 เทเลพอร์ตไปหาคนที่มึงเลือก",
    Desc = "ใส่ชื่อเสร็จก็กดเลย",
    Callback = function()
        if selectedPlayerName == "" then
            Window:Notify({Title = "❌ Error", Desc = "พิมชื่อก่อน!", Time = 3})
            return
        end

        local input = string.lower(selectedPlayerName)
        if #input < 3 then
            Window:Notify({Title = "❌ Error", Desc = "พิมพ์อย่างน้อย 3 ตัวอักษร", Time = 3})
            return
        end

        local target
        for _, p in pairs(Players:GetPlayers()) do
            local name, displayName = string.lower(p.Name), string.lower(p.DisplayName)
            if name:sub(1, #input) == input or displayName:sub(1, #input) == input then
                target = p
                break
            end
        end

        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
            Window:Notify({Title = "✅ สำเร็จ", Desc = "เทเลพอร์ตไปหา " .. target.DisplayName .. " แล้ว!", Time = 3})
        else
            Window:Notify({Title = "❌ Error", Desc = "ไม่พบผู้เล่นที่ชื่อ '" .. selectedPlayerName .. "'", Time = 3})
        end
    end
})

local PlayerListBox = TPTab:Code({
    Title = "รายชื่อคนในเซิฟตอนนี้👥",
    Code = table.concat(playerNames, "\n")
})
task.spawn(function()
    while task.wait(2) do
        UpdatePlayerList()
        PlayerListBox:SetCode(table.concat(playerNames, "\n"))
    end
end)

local CreditTab = Window:Tab({Title = "Credits", Icon = "heart"}) do
    CreditTab:Section({Title = "ผู้พัฒนา"})
    CreditTab:Button({
        Title = "📋 Copy Discord Link",
        Desc = "คัดลอกลิงก์ Discord ของ ERROR HUB",
        Callback = function()
            setclipboard("https://discord.gg/eZwAXpwAt")
            Window:Notify({Title = "Copied!", Desc = "ลิงก์ Discord ถูกคัดลอกแล้ว ✅", Time = 3})
        end
    })
end

Window:Notify({
    Title = "ERROR HUB",
    Desc = "✅ Loaded successfully!",
    Time = 6
})
